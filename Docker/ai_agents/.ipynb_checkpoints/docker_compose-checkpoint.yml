version: "3.8"

services:
  agent:
    build: ./agent
    image: gemini_agent:latest
    container_name: gemini_agent
    ports:
      - "8000:8000"
    env_file:
      - D:/Gemini/.env   # full Windows path to your env file
    volumes:
      - agent_data:/app

volumes:
  agent_data:
